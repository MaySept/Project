#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/sh" ]; then
  "$basedir//bin/sh"  "$basedir/../.0.0.3@md5-js/bin/cli.js" "$@"
  ret=$?
else 
  /bin/sh  "$basedir/../.0.0.3@md5-js/bin/cli.js" "$@"
  ret=$?
fi
exit $ret
